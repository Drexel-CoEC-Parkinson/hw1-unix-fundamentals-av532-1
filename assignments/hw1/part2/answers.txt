# CS 503 — HW1 Part 2: File Permissions
#
# SECTION A: chmod Commands
# For each file, write the exact chmod command you used to fix its
# permissions. One command per file.
#
# Example:
#   some_file.txt: chmod 644 some_file.txt
# ====================================================================

deploy.sh: chmod 750 part2/deploy.sh

database_backup.sh: chmod 700 part2/database_backup.sh

api_keys.conf: chmod 600 part2/api_keys.conf

shared_data.csv: chmod 640 part2/shared_data.csv

report_generator.py: chmod 750 part2/report_generator.py

experiment_log.txt: chmod 444 part2/experiment_log.txt

project_dir/: 750 part2/project_dir

run_tests.sh: chmod 755 part2/run_tests.sh


# ====================================================================
# SECTION B: Conceptual Questions
# Answer each question in a few sentences.
# ====================================================================

# Q2a. A file has permissions rw-r----- and is owned by user alice and
# group mlteam. Can user bob, who is a member of mlteam, execute the
# file? Can he read it?

Q2a: Bob can only read the file, not execute it because his permissions are r--


# Q2b. You run ls -l and see -rwxrwxrwx on a file containing your API
# keys. Explain why this is a problem and what permissions you would
# set instead. Give the chmod command you would use.

Q2b: This is a problem because anyone has the ability to see or use the API key. Permissiosn should be rw-------. chmod 600 api_keys.conf


# Q2c. A directory has permissions rwxr-xr-x. You are not the owner
# and not in the group. Can you list the directory's contents? Can you
# create a new file inside it? Explain why or why not.

Q2c: You can list the directory because other have r-x. You cannot create a file within because you do not have write permissiions.


# Q2d. What is the difference between chmod 755 and chmod u=rwx,go=rx?

Q2d: They are the same except chmod 755 uses numeric notation where the other uses symbolic notation.

